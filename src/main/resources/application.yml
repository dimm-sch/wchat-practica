spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5400/wchat
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

logging:
  level:
    web: debug